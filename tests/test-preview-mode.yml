name: Test deploy-pages preview mode (pull request)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: pull_request
      GITHUB_REF: refs/pull/1/merge
      GITHUB_HEAD_REF: feature-branch
      GITHUB_BASE_REF: main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Invoke deploy-pages in preview mode (expect failure - no real Pages environment)
        id: action
        uses: .
        with:
          token: fake-token-for-testing
          timeout: '5000'
          error_count: '1'
          reporting_interval: '1000'
          artifact_name: 'github-pages'
          preview: 'true'
        continue-on-error: true

      - name: Assert action ran and failed gracefully in preview mode
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action should fail (no real GitHub Pages environment / preview not publicly available)
          # but not crash unexpectedly
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: action succeeded (unexpected but acceptable)"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully as expected in non-Pages environment"
          else
            echo "FAIL: unexpected outcome: $OUTCOME"
            exit 1
          fi
